{
  "name": "blackforest-holidays",
  "version": "1.0.0",
  "description": "Blackforest Holidays - Full Stack Travel Platform & CMS Admin Panel",
  "private": true,
  "main": "server/src/server.js",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "install:all": "npm --prefix server install && npm --prefix client install",
    "install:server": "npm --prefix server install",
    "install:client": "npm --prefix client install",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "build:client": "npm --prefix client run build",
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/src/server.js"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": ["travel", "tourism", "cms", "mysql", "express", "react", "node"],
  "author": "Blackforest Holidays",
  "license": "ISC"
}
